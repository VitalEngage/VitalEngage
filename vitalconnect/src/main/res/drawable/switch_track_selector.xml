<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Track color when switch is checked -->
    <item android:color="@color/colorEetOpacity" android:state_checked="true" />
    <!-- Track color when switch is unchecked -->
    <item android:color="@color/divider" />
</selector>