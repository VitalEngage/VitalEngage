<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state -->
    <item android:state_enabled="false" android:color="@color/colorEetOpacity" />
    <!-- Pressed state -->
    <item android:state_pressed="true" android:color="@color/colorEet" />
    <!-- Default state (enabled and normal) -->
    <item android:color="@color/colorEet" />
</selector>